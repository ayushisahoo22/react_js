function variables(){
    const name = "ayushi";
    const age = 18;

    //expressions are enclosed in {}
    // return part is enclosed in ()
    return (
        <h3>I am {name} of {age} years old {/*this is for comment*/}</h3>
    )
}

export default variables;